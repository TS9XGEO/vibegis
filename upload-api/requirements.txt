fastapi
uvicorn[standard]
python-multipart
geopandas
shapely
pyproj
psycopg[binary]
sqlalchemy
geoalchemy2
pyyaml
pyjwt
bcrypt
cryptography
anthropic
openai
python-json-logger
# probe_pointcloud() reads LAS/LAZ headers in-process (CRS, bounds, point
# count, RGB/classification presence) before handing the file to py3dtiles,
# which lives in its own venv. lazrs is a prebuilt Rust wheel — it gives LAZ
# support without an apt-level liblaszip, keeping "nothing to install on the
# host" true for the image too.
laspy[lazrs]
