# Auto-provisions the Loki datasource on Grafana startup — no manual "Add
# data source" click needed. See docker-compose.yml's grafana service.
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    uid: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: true
