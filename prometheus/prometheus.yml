# Scrapes docker-stats-exporter for per-container CPU/memory/network/disk
# metrics. Bind-mounted into the prometheus service — see docker-compose.yml.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: docker-stats-exporter
    static_configs:
      - targets: ["docker-stats-exporter:9100"]
